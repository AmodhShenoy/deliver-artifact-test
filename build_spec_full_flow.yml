version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
failImmediatelyOnError: true
steps:
  - type: Command
    name: "Test fail"
    onFailure:
    onFailure:
      - type: Command
        command: |
          echo "RUNNING ON FAILURE"
        command: |
          exit 1
